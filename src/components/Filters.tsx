import { SlidersHorizontal } from "@phosphor-icons/react";

const Filters = () => {
  return (
    <div className="p-4 bg-deep-lagoon-blue text-white rounded-2xl w-fit">
      <SlidersHorizontal size={24} weight="bold"/>
    </div>
  );
};

export default Filters;
