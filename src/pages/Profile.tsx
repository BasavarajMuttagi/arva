import ProfileLayout from "../layouts/ProfileLayout";

const Profile = () => {
  return (
    <main>
      <p className="text-center p-2 text-xl font-semibold">Profile</p>
      <ProfileLayout />
    </main>
  );
};

export default Profile;
