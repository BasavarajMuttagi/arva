import HomeLayout from "../layouts/HomeLayout";
import MainLayout from "../layouts/MainLayout";

const Home = () => {
  return (
    <MainLayout>
      <HomeLayout />
    </MainLayout>
  );
};

export default Home;
